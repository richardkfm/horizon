"""Server-rendered web UI (Jinja2 + HTMX + Alpine.js).

Kept intentionally lightweight: no build step, vendored JS, and a print/
low-power friendly stylesheet so the UI works on e-ink and old displays.
"""
