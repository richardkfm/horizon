"""Read-only Knowledge API and AI API.

These endpoints form horizon's stable integration surface. External projects
(e.g. neighbourgood) may consume them, so request/response shapes should remain
backward-compatible. horizon never depends on its consumers.
"""
